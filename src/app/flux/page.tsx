import Flux from '@/components/Flux';

export const metadata = {
  title: 'Flux · Journey — Fixora',
  description: 'A luminous particle journey through six stations of form and light.',
};

export default function FluxPage() {
  return <Flux />;
}
